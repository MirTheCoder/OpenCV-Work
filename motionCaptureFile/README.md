Installations:

For testing the RPi.GPIO, make sure to install fake_rpi
by typing "pip install fake_rpi", this is because you technically
need and actually raspberry PI system to use RPi.GPIO, so in this case
we will fake it so that the code will still run but nothing will happen
or take place.